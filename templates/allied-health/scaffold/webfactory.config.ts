import { defineProject } from '@webfactory/core';

export default defineProject({
  name: '{{projectName}}',
  slug: '{{project}}',
  template: '{{template}}',
  theme: '{{theme}}',
  pages: {{pages}},
});
