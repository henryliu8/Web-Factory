---
import StructuredData from "@webfactory/seo/StructuredData.astro";

export interface Props {
  schema: Record<string, unknown> | Array<Record<string, unknown>>;
}
---

<StructuredData schema={Astro.props.schema} />
