---
import Item from '@webfactory/template-astrowind/components/blog/ListItem.astro';
import type { Post } from '@webfactory/template-astrowind/types';

export interface Props {
  posts: Array<Post>;
}

const { posts } = Astro.props;
---

<ul>
  {
    posts.map((post) => (
      <li class="mb-12 md:mb-20">
        <Item post={post} />
      </li>
    ))
  }
</ul>
