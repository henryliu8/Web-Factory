---
import SiteLayout from './SiteLayout.astro';

const { title, description, ...props } = Astro.props;
---

<SiteLayout title={title ?? 'Web Factory site'} description={description} {...props}>
  <slot />
</SiteLayout>
