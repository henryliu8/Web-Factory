import { defineProject } from "@webfactory/core";

export default defineProject({
  name: "Rising Tree",
  slug: "rising-tree",
  template: "allied-health",
  theme: "modern-clinical",
  pages: ["home", "about-us", "services", "team", "contact"],
});
