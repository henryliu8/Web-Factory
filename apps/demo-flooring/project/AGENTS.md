# Project Agent Instructions

- Read the repository `AGENTS.md`, `WEB_FACTORY_REQUIREMENTS.md`, and this project's files before editing.
- Preserve `Project > Theme > Template > Shared` precedence.
- Keep Grain & Ground branding and content inside `apps/demo-flooring`.
- Store page copy and repeated page data in `src/content`; do not move it into shared components.
- Use the project Header, Footer, and HomeSlider through `ProjectShell` rather than copying their markup into pages.
- Keep interactions keyboard accessible and respect `prefers-reduced-motion`.
- Treat all business details as fictional demonstration data.
- Run the project Astro check and build after changes.
