# Modern Clinical
Reusable calm green, mint and cream palette with serif headings. No business content or assets.
The public `styles` export supplies semantic tokens; the selected template owns layout and responsive rules. Project CSS loads last. No markup overrides are required.
